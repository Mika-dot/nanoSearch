using NLog;

namespace nanoSearchWebAPI
{/// <summary>
 /// Логер для записи всех ключевых этапов работы надстройки
 /// </summary>
    public static class Looger
    {
        public static Logger Logger = LogManager.GetCurrentClassLogger();
    }
}
